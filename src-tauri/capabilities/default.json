{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main window (P0 scaffold).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
